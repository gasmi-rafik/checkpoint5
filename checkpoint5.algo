ALGORITHM 
int n = strlen(str); 
  
// An empty string is  
// considered as palindrome 
IF (n == 0) THEN
    return true; 
END_IF () 
    
  
 test(str, 0, n - 1); 
 
  

    str STRING[] = "geeg"; 
  
    if (test(str)) 
    Write("Yes"); 
    else
    Write("No"); 
 
END

FUNCTION test(char STRING[],  
              int s, int e) : BOOLEAN
VAR
BEGIN
     if (s == e) 
    return true; 
    else  
    return false; 
    if (s < e + 1) 
    return test(str, s + 1, e - 1);
END